{
  "name": "1-commit",
  "version": "1.0.0",
  "description": "This project is just a demo how to use Git\r //git init (tạo priject mới)\r //git status\r //git add\r //git commit (git commit -m 'mo ta commit')",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0"
  }
}
